---

apiVersion: v1
kind: Service

metadata:
  name: web
  namespace: default

spec:
  type: ClusterIP
  selector:
    app: web
    tier: frontend
    track: stable
  ports:
    - name: http
      protocol: TCP
      port: 80
