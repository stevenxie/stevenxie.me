{
  "name": "stevenxie.me",
  "private": true,
  "scripts": {
    "dev": "gridsome develop",
    "lint": "cross-env NODE_ENV=production eslint --fix --max-warnings 0 --ignore-path .prettierignore . && prettier --write -l './**/*.{js,vue,css,scss}'",
    "build": "gridsome build",
    "clean": "rimraf ./dist ./src/.temp",
    "explore": "gridsome explore"
  },
  "dependencies": {
    "gridsome": "^0.7.0"
  },
  "devDependencies": {
    "cross-env": "^6.0.3",
    "eslint": "^5.0.0",
    "eslint-config-prettier": "^6.4.0",
    "eslint-plugin-gridsome": "^1.3.2",
    "prettier": "^1.18.2",
    "rimraf": "^3.0.0",
    "vue-eslint-parser": "^6.0.4"
  }
}
